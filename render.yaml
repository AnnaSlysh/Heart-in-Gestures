services:
  - type: web
    name: gamemodel1
    env: python
    pythonVersion: "3.11.0"
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run pages/Main.py --server.port $PORT --server.address 0.0.0.0 --server.headless true
    plan: free
